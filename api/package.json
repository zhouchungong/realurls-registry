{
  "name": "realurls-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "test": "node --test ../packages/core/resolve.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
