# Contributing

中文版：[docs/zh/CONTRIBUTING.md](docs/zh/CONTRIBUTING.md)

## The one rule that matters

**You contribute leads, not data.**

Every YAML file under `entities/` is generated by the pipeline. **Pull requests that edit them by hand are rejected by CI.**

This is not process for its own sake. If data could be changed by a pull request, this repository would be the single most valuable target for anyone trying to launder a phishing domain, and "a carefully crafted PR slips past a tired reviewer" is a certainty over time. Taking humans off the write path is the only reliable defence. See [SECURITY.md](SECURITY.md).

---

## I want a tool or company listed

Open a [lead issue](../../issues/new?template=submit-domain.yml) with three things:

1. the organization's name and the domain you believe is its official site;
2. how you know (its GitHub organization? an npm package? its verified social account?);
3. the category.

A bot runs the full evidence collection and then either:

- **enough evidence** → opens a pull request automatically; a human reviews and merges;
- **not enough** → closes the issue and tells you **exactly which piece is missing**, so you can go find it.

You do not need to run WHOIS, take screenshots or write YAML. **Collecting evidence is our job, not yours.**

## I control the domain

You have the final say. A DNS TXT self-attestation overrides any verdict of ours:

```
_realurls.example.com.   TXT   "realurls-site-verification=<token>"
```

State who you are in an issue and we will issue a token. This is evidence A5: the highest weight (0.90) and the only anchor that waives the "domain younger than 180 days" floor.

## I think you got something wrong

**Please tell us.** This is the most serious class of issue we have.

Open a `dispute` issue or email `dispute@realurls.org`. Within **48 hours** the record is downgraded to `disputed` and positive API answers stop — harm first, investigation second. **The burden of proof is on us, not on you.**

## I want to change code

Welcome. Two kinds of change, two bars:

| change | requirement |
|---|---|
| collectors, build scripts, docs, tooling | normal PR; CI must pass |
| **thresholds or weights in `src/policy.py`** | this changes the public trust promise: update POLICY.md in the same PR, two-person approval via CODEOWNERS, the full adversarial corpus must pass, and state how many records move up or down. See [POLICY.md §4](POLICY.md) |

**Changes that lower the bar (make more things verified) deserve extra suspicion** — that is precisely what an attacker wants.

### Fixed a false positive?

**Add a case to `tests/negative_corpus.yaml`.** A false-positive fix without a new adversarial case will not be merged; without it the same class of error will come back.

## Development

```bash
pip install -e ".[dev]"
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest tests -q
ruff check src tests
```

## Conduct

Argue about evidence, not motives.

If you suspect a record was submitted in bad faith, say so with your reasoning and evidence — we will take it seriously. Accusations without evidence will not be entertained.
