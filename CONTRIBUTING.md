# 如何贡献

## 最重要的一条规则

**你贡献的是「线索」，不是「数据」。**

`entities/` 目录下的所有 YAML 由流水线自动生成，**不接受人类直接编辑的 PR**（CI 会拒绝）。

这不是流程洁癖。如果数据可以被 PR 直接改，本仓库就是黑产洗白钓鱼站的最高价值目标，而「精心构造的 PR 骗过疲惫的 reviewer」是必然会发生的事。把人从数据写入路径上移开，是唯一可靠的防御。详见 [SECURITY.md](SECURITY.md)。

---

## 我想收录一个工具 / 公司

开一个 [收录线索 Issue](../../issues/new?template=submit-domain.yml)，填三样东西：

1. 组织名与你认为的官网域名
2. 你是怎么知道的（GitHub 组织？npm 包？官方社媒？）
3. 类别

机器人会自动跑全套证据采集，然后：

- **证据充分** → 自动开 PR，人类 review 后合并
- **证据不足** → 自动关闭 Issue，并明确告诉你**缺哪一条**（这样你可以去补）

你不需要自己查 WHOIS、不需要截图、不需要写 YAML。**采证是我们的工作，不是你的。**

## 我是某域名的实际控制者

你对自己的域名有最终解释权。做 DNS TXT 自证即可覆盖我们的任何判定：

```
_realurls.example.com.   TXT   "realurls-site-verification=<token>"
```

在 Issue 里说明你的身份，我们会给你 token。这是 A5 证据，权重最高（0.90），且是唯一能豁免「域名注册未满 180 天」硬门槛的证据。

## 我发现你们标错了

**请一定告诉我们。** 这是本项目最严重的一类问题。

开一个 `dispute` Issue，或发信 `dispute@realurls.com`。我们承诺 **48 小时内**把争议记录降级为 `disputed` 并暂停 API 肯定答复 —— 先止损，再查证。**举证责任在我们，不在你。**

## 我想改代码

欢迎。但注意两类改动的门槛不同：

| 改动 | 要求 |
|---|---|
| collectors、构建脚本、文档、工具 | 普通 PR 流程，跑过 CI 即可 |
| **`src/policy.py` 的阈值或权重** | 改动的是本项目对外的信任承诺。需：同步改 POLICY.md + CODEOWNERS 双人审批 + 全量负样本回归 + 说明本次改动会让多少条记录升级/降级。见 [POLICY.md §4](POLICY.md) |

**降低门槛的改动（让更多东西变成 verified）需要格外谨慎** —— 这正是攻击者最想让我们做的事。

### 修了一个误报？

**必须往 `tests/negative_corpus.yaml` 里加一条对应用例。** 没有新增用例的误报修复 PR 不会被合并 —— 否则同类问题一定会复发。

## 开发

```bash
pip install -e ".[dev]"
pytest tests -q
ruff check src tests
```

## 行为准则

对事不对人。讨论证据，不讨论动机。

如果你怀疑某条记录是被恶意提交的，直接说出你的理由和证据 —— 我们会认真对待，但不接受无证据的人身指控。
